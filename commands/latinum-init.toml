description = "Drop the always-on latinum activation rule into the current repo for every IDE agent"
prompt = "Run `node src/tools/latinum-init.js {{args}}` in the current repo and report the detailed result. Use --dry-run first if the user did not pass --force, so we never silently overwrite an existing rule file."
