---
description: Show latinum lifetime token-earnings stats
---
Show latinum stats — total tokens generated, sessions, estimated profit.

Read the lifetime history log at `~/.config/latinum/.latinum-history.jsonl`
(or wherever the latinum-stats script writes it). Output: total generated,
sessions counted, avg expansion ratio, estimated profit in USD.
